BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Dumpster] DROP CONSTRAINT [Dumpster_dumpster_ownerTypeID_df];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
